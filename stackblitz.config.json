{
  "installDependencies": true,
  "startCommand": "yarn install && cd packages/nextjs && npx next dev",
  "env": {
    "NODE_ENV": "development",
    "STACKBLITZ": "true",
    "SKIP_ENV_VALIDATION": "true"
  }
}