{
  "installDependencies": true,
  "startCommand": "cd packages/nextjs && npx --yes next dev",
  "env": {
    "NODE_ENV": "development",
    "STACKBLITZ": "true",
    "SKIP_ENV_VALIDATION": "true"
  }
}